# Copy to .env and fill in. The .env file is gitignored (never commit secrets).
# routin.ai gateway — single channel for v1 (docs/02_adapter.md).
ROUTIN_API_KEY=

# Optional overrides (defaults mirror the verified PoC endpoints):
# ROUTIN_TEXT_MODEL=deepseek-v4-pro
# ROUTIN_IMAGE_MODEL=gpt-5.4
# ROUTIN_VIDEO_MODEL=doubao-seedance-2-0-fast-260128
# DATABASE_URL=sqlite:///ai_drama.db
